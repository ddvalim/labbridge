import React from 'react';
import './styles.css';

const Escopo = () => (
    <div id='escopo'>
        <h1> Escopo do problema</h1>
        <p id='texto'>O desafio envolve a implementação de funcionalidades web tanto no frontend quanto no
    backend. Você deve utilizar a linguagem Java para o backend e pode utilizar as linguagens,
    ferramentas e frameworks que achar mais adequados para o frontend.</p>
    </div>
)

export default Escopo;